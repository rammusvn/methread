name: MeBlox Test

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: node:20-alpine
    steps:
      - run: echo "ci is running"
      - uses: actions/checkout@v2
      - run: npm ci
      - run: npm test
